schema_version = "1.3"
artifacts = [
  { source = "/app/design.sv", destination = "design.sv" },
]

[task]
name = "svgap/reset-events"
description = "Generate reset-safe RTL and expose paired functional and structural evidence."
keywords = ["systemverilog", "rtl", "reset", "rdc", "structural-validity"]
[[task.authors]]
name = "Shraddha Sridhar"
email = "shsridhar@nvidia.com"

[metadata]
source = "SV-Gap"
source_taskpack = "reset-replication-v0.2"
source_task = "reset_events"
svgap_version = "0.3.0a6"
category = "hardware-evaluation"
difficulty = "focused"

[verifier]
timeout_sec = 180.0
network_mode = "no-network"

[verifier.env]

[agent]
timeout_sec = 900.0
network_mode = "public"

[environment]
network_mode = "public"
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 10240
gpus = 0
mcp_servers = []

[environment.env]

[solution.env]
